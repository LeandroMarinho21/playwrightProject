{
  "name": "playwright-bdd-mcp-starter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "npx bddgen",
    "test": "npm run generate && npx playwright test",
    "test:ui": "npm run generate && npx playwright test --ui",
    "report": "npx playwright show-report",
    "install-browsers": "npx playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.12.7",
    "playwright-bdd": "^8.4.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}

